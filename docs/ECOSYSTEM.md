# Экосистема Godot: что изучено, что взято, что проверено

Разбор списка ресурсов от 2026-08-23. Всё, что можно было проверить фактами (актуальность,
совместимость с 4.7, лицензии, размеры), — проверено, а не пересказано.

Клоны лежат в `docs/ecosystem/` и **не хранятся в git** — восстанавливаются
`bash docs/fetch_ecosystem.sh`.

## Итог одной таблицей

| Ресурс | Статус на 2026-08 | Совместимость с 4.7 | Взято локально |
|---|---|---|---|
| Godot Official Docs | активен | ветка `4.7` | ✅ `docs/godot-docs/` |
| Awesome Godot | активен (июль 2026) | каталог, версии разные | ✅ `docs/ecosystem/awesome-godot/` |
| GDQuest (обучение, инструменты) | активен | 4.x | частично, см. ниже |
| **gdquest-demos** (демо и персонажи) | активен (август 2026) | проекты уже на 4.7 | ⚠️ только `2d-builder` (MIT); арт **не берём** |
| Godot Recipes (KidsCanCode) | **заморожен с июля 2024** | эпоха 4.0–4.2 | ❌ читать выборочно онлайн |
| Godotneers (YouTube) | активен | — | ❌ видео, офлайн не взять |
| GodotShaders.com | активен | у каждого шейдера своя | ❌ брать точечно |
| StayAtHomeDev (YouTube) | активен | — | ❌ видео |
| Phantom Camera | активен (август 2026) | **да, чинили под 4.7.1** | ✅ `docs/ecosystem/phantom-camera/` |
| Dialogic | активен, 2.0-**alpha** | заявлено «Godot 4.5+» | ❌ по потребности |
| Terrain3D | активен | релиз заявляет 4.4–4.6, **4.7 только в `main`** | ❌ по потребности |

## Что из этого действительно ценно нам

### gdquest-demos — арт не берём, лицензия не та

Код у GDQuest под MIT, а **арт (модели, текстуры) — CC-BY-NC-SA 4.0**: некоммерческая
и «заражающая» share-alike. Решение по проекту: **лицензионно ограниченное не тянем вообще**,
чтобы оно физически не могло попасть в сборку. Клон персонажей и TPS-контроллера удалён,
из `fetch_ecosystem.sh` убран.

Взято только **`godot-2d-builder`** (MIT целиком, 3.5 МБ) — симуляция в духе Factorio:
инвентарь, перетаскивание предметов, сетка. Полезный источник паттернов для 2D-UI.

Эталонные цифры правильного персонажа (снял с их Sophia до удаления, как ориентир для
собственных ассетов): 1.36 × 1.82 × 1.21 м, пивот ровно в основании, 19 086 треугольников,
4 материала, 1 скелет, 8 анимаций (Idle, Run, Jump, Fall, WallSlide, EdgeGrab, RunTiltL/R).

### Phantom Camera — берём, когда дойдём до камеры

Аналог Cinemachine: follow/look-at цели, тви́ны переходов, зоны, тряска. Релиз v0.11.0.3
(июль 2026) в примечаниях явно чинит регрессию, появившуюся **в Godot 4.7.1**, — то есть
аддон живёт в ногу с нашей версией. Лицензия MIT, ставится копированием
`addons/phantom_camera` в проект.

Осмысленно, когда камера сложнее «прицепить к игроку»: плечевой вид, переключение целей,
кинематографичные переходы. Для статичной камеры сверху — избыточно.

### Awesome Godot — каталог, а не библиотека

Список из ~10.6k звёзд, обновлён в июле 2026. Полезен как «а есть ли уже готовое»:
`grep -i <тема> docs/ecosystem/awesome-godot/README.md`. Версии плагинов там не указаны —
каждый кандидат всё равно надо проверять отдельно.

### Godot Recipes (KidsCanCode) — осторожно, устарело

Репозиторий не трогали с **июля 2024**, то есть весь код писался под Godot 4.0–4.2.
С тех пор сменились дефолты (D3D12, Jolt, stretch), `TileMap` объявлен устаревшим,
поменялись сигнатуры. Концептуально рецепты (машина состояний, инвентарь, камера)
остаются полезными, но **код оттуда нельзя копировать не сверив с `docs/godot-docs/`**.
Сайт: https://kidscancode.org/godot_recipes/4.x/

### GodotShaders.com — точечно и с оглядкой на лицензию

Крупнейшая база готовых шейдеров. Скачивать целиком смысла нет: берём конкретный эффект
под конкретную задачу. У каждого шейдера **своя лицензия** (часто MIT или CC0, но не
всегда) — проверять на странице эффекта перед тем, как класть в игру.

### Dialogic — только если будет много диалогов

Версия всё ещё **2.0-alpha** (alpha-21, июль 2026), в манифесте заявлено «Godot 4.5+».
Для игры с парой реплик это лишняя зависимость в состоянии alpha; для визуальной новеллы
или RPG с ветвлением — экономит недели. MIT.

`git clone --depth 1 https://github.com/dialogic-godot/dialogic`

### Terrain3D — мощно, но версия под 4.7 не в релизе

GDExtension (C++), редактируемый ландшафт с высокой производительностью. Последний
стабильный релиз v1.0.2 (май 2026) заявляет поддержку **Godot 4.4–4.6**; запрос на
обновление под 4.7 в трекере закрыт в июле 2026, то есть работа ушла в `main`, но
стабильного релиза под 4.7 на момент проверки нет.

Вывод: если понадобится большой ландшафт — брать `main` и **сразу проверять на 4.7**,
закладывая риск. Для процедурной геометрии из кода (наш обычный подход) он не нужен.

### GDQuest: инструменты, которые могут пригодиться

- **GDScript-formatter** — форматтер кода, активно развивается.
  Полезен, если захотим единый стиль без ручной вычитки.
- **learn-gdscript** — интерактивный курс; нам как справочник не нужен, у нас есть мануал.

### Видео-каналы

Godotneers и StayAtHomeDev офлайн не заберёшь. Их сильные стороны стоит помнить как
направление поиска, когда упрёмся:

- **Godotneers** — структура сцен, UI-пайплайны, векторная математика, оптимизация.
- **StayAtHomeDev** — 3D-графика: освещение, SDFGI, объёмный туман, интеграция ассетов.

Но первым делом всё равно проверять `docs/godot-docs/` — там те же темы, только
в актуальной для 4.7 версии и с возможностью грепа.

## Что проверено на практике

Пока изучал, прогнал две вещи через настоящий движок:

1. **Импорт `.glb` работает** — `godot-4-3D-Characters` импортировался в 4.7 без ошибок
   (`--headless --import`, код возврата 0). Это снимает главный риск пайплайна с Houdini:
   раньше ни один наш проект `.glb` не импортировал.
   Побочный факт: Godot при импорте `.glb` со встроенными текстурами **извлекает их
   файлами рядом** — в приёмке это нормально, а не «Houdini прислала лишнее».

2. **Собран инструмент приёмки** — `tools/asset_check/`:

   ```bash
   bash tools/asset_check/check.sh pipeline/delivery/task-0007-lava/v1
   ```

   Печатает паспорт: габариты в метрах, полигонаж, число материалов, скелеты, список
   анимаций, размеры текстур — и замечания по `pipeline/ASSET_SPEC.md` (сантиметры вместо
   метров, пивот не на месте, лишние материалы, текстура не степень двойки).

   Проверен на Sophia из набора GDQuest: 1.36 × 1.82 × 1.21 м, пивот ровно в основании,
   19 086 треугольников, 1 скелет, 8 анимаций (Idle, Run, Jump, Fall, WallSlide,
   EdgeGrab, RunTiltL/R). Это заодно и эталон того, как должен выглядеть правильный
   персонаж для 4.7.

   Чего инструмент **не** проверяет: конвенцию нормалей (Y+ против Y−), качество развёртки
   и «красиво ли» — это только глазами.

## Как восстановить клоны

```bash
bash docs/fetch_ecosystem.sh          # то, что взято локально
bash docs/fetch_ecosystem.sh --all    # плюс тяжёлые: Dialogic, Terrain3D
```


## Чего у нас пока нет

Правило проекта: **лицензионно ограниченное не тянем**. Ниже — только CC0 / MIT / OFL.

### 1. Временные ассеты-заглушки (нужно раньше всего)

Houdini-сессия выдаёт ассеты не мгновенно, а собирать сцену на голых кубах неудобно.
Нужен набор болванок, которыми можно занять место, пока идёт настоящий ассет:

- **Kenney** (https://kenney.nl/assets) — **CC0**, коммерческое использование без указания
  авторства. 3D-киты (город, подземелье, машины), UI-наборы, иконки, звуки.
- **Quaternius** (https://quaternius.com) — **CC0**, 60–70 % паков бесплатны. Стилизованные
  3D-модели, в том числе **риггованные и анимированные персонажи**.

Это же закрывает дыру, образовавшуюся после отказа от арта GDQuest.

### 2. Анимации персонажей

Houdini — не про риггинг и анимацию гуманоидов, а в обоих скилах этой темы нет вообще.
Если в игре будет ходящий персонаж, брать анимации придётся со стороны:

- **Quaternius Universal Animation Library** — CC0, ретаргетируемые анимации.
- Ретаргетинг средствами движка описан в
  `docs/godot-docs/tutorials/assets_pipeline/retargeting_3d_skeletons.rst`.

Самый дешёвый вариант — выбрать игру, где гуманоидов нет вовсе (техника, корабли, дроны,
существа с процедурной анимацией). Тогда пункт отпадает целиком.

### 3. Звук и музыка

В скиле есть процедурный синтез (шум дождя, клики, бипы) — этого хватает на прототип,
но не на игру. Свободные источники: наборы звуков **Kenney (CC0)**, **Freesound** с
фильтром по CC0, **Sonniss GDC** (royalty-free архивы). Музыку проще заказать/сгенерировать
отдельно — Houdini этого не делает.

### 4. Тесты

Своя самопроверка (`--headless --quit-after`, скриншот-харнесс) ловит падения и регресс
визуала, но юнит-тестов на алгоритмы (генерация уровня, инвентарь, баланс) у нас нет.

- **gdUnit4** — MIT, активен (август 2026): https://github.com/godot-gdunit-labs/gdUnit4
- **GUT** — активен (август 2026), v9.6.1: https://github.com/bitwes/Gut

Оба ставятся как аддон и умеют гоняться из CLI, то есть вписываются в наш цикл.

### 5. Форматтер и линтер GDScript

**gdtoolkit** (`gdformat`, `gdlint`, MIT, ставится через pip) — но последний коммит
**октябрь 2025**, релиз 4.5.0: синтаксиса 4.6/4.7 он может не знать. Ставить только если
припечёт единообразие, и проверять, что он не портит свежий синтаксис.
Альтернатива — **GDScript-formatter** от GDQuest, он активно развивается.

### 6. Что уже НЕ нужно (проверено)

- **Шрифт с кириллицей** — не нужен. Встроенный **Open Sans SemiBold** содержит кириллицу:
  проверил `Font.has_char()` на реальном движке, `Я`, `ж`, `—`, `€` есть. Нет только эмодзи —
  под них понадобится fallback-шрифт (Noto Emoji, OFL).
- **Импорт `.glb`** — работает, проверено.

### 7. Инфраструктура, когда дойдут руки

- **Шаблоны экспорта под Web/Linux** — стоят только Windows. Ставятся из CLI (см. `CLAUDE.md`).
- **CI на GitHub Actions** — прогонять `--headless --quit-after` на каждый пуш, чтобы
  сломанный скрипт не доезжал до сборки. Дёшево и окупается.
- **Git LFS** — если ассеты от Houdini перерастут лимиты протокола (50 МБ на файл).

### 8. Главное, чего не хватает, — не техническое

**Не выбрана игра.** Пока нет жанра, Houdini-сессии нечего поручить: любое задание на
ассет требует знать, что за мир, какой вид и насколько близко игрок это увидит.
