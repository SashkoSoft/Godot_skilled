# Эталонный риг: `smpl_rig.glb`

Скелет, на котором договариваются роль `aimocap`, роль `houdini-assets` и движок.
Один и тот же для всех: анимации приезжают на нём, модель персонажа привязывается
к нему же.

## Что это

Скелет **SMPL**, 22 кости — ровно то, что отдаёт мокап-пайплайн
`C:\Users\Papa\Documents\AImocap` (GVHMR, SIGGRAPH Asia 2024).

Файл **не правится руками**: он пересобирается из самой модели SMPL
инструментом `tools/rig/make_smpl_rig.py`. Поза покоя берётся из шаблона
(`J = J_regressor @ v_template`), а не конструируется на глаз — повороты,
которые отдаёт GVHMR, заданы относительно этого шаблона, и своя поза покоя
означала бы систематический сдвиг в каждой анимации.

Замер по самому файлу (bind-поза, метры, Y вверх):

```
костей: 22, скелетов: 1, мешей: 0, анимаций: 0
поза покоя: T-поза, запястья на одной высоте (1.384 и 1.381)
кости по Y: 0.018 .. 1.514 (таз 0.939, шея 1.357)
подошва на нуле: низ МЕША шаблона = 0.000, стопа-кость = 0.018
лицом в −Z: носок стопы (z = −0.093) впереди щиколотки (z = +0.027)
левая сторона по −X: left_wrist x = −0.681
ошибка зеркала: 8.9 мм (худшая — колено)
```

**Про 8.9 мм.** Это собственная асимметрия шаблона SMPL: он усреднённое
тело, а не чертёж. Исправлять её нельзя — симметризация увела бы позу покоя
от шаблона и вернула бы тот самый сдвиг в анимациях. Считать риг
симметричным с точностью до сантиметра.

`head` — сустав у основания черепа, поэтому 1.514 это не рост. Рост по коже
шаблона **1.717 м**, на него и рассчитывать модель.

Y вверх, вперёд −Z, правая рука по +X — доворачивать ничего не надо.

## История: чем был плох файл до 14 сентября 2026

Прежний `smpl_rig.glb` был собран из мокап-выгрузки, и в нём лежала **не
поза покоя, а кадр движения** — повороты запечены в каждую кость. Нашла это
роль `houdini-assets` при сдаче task-0039; перемер подтвердил:

| замер | было | стало |
|---|---|---|
| худшая ошибка зеркала | 151.7 мм (стопа) | 8.9 мм |
| щиколотки по высоте | 0.061 / 0.135, разница 73.7 мм | разница 6.9 мм |
| наклон линии плеч | 6.1° | 1.0° |
| длина ноги слева/справа | 0.773 / 0.762 | 0.777 / 0.786 |

Персонаж, собранный на таком риге, наследовал шагающую асимметричную
привязку, и низ его меша уходил на −0.06 под ноль — не по вине исполнителя.
Отсюда правило выше: риг пересобирается скриптом из модели, не правится.

## Кости — полный список и иерархия

```
pelvis
├── left_hip → left_knee → left_ankle → left_foot
├── right_hip → right_knee → right_ankle → right_foot
└── spine1 → spine2 → spine3
    ├── neck → head
    ├── left_collar → left_shoulder → left_elbow → left_wrist
    └── right_collar → right_shoulder → right_elbow → right_wrist
```

Кистей, пальцев и лицевых костей в SMPL-22 нет. Понадобятся — это отдельный
разговор и другой скелет (SMPL-X), а не «добавлю парочку».

## Правила

- Кости **не переименовывать**, не добавлять, не удалять, позу покоя не менять.
  Godot сравнивает позы покоя при переносе анимаций между скелетами: разойдутся —
  анимация поедет.
- Масштаб в костях = 1.
- Это **не персонаж**. Персонаж — отдельная модель от `houdini-assets`,
  привязанная к этому же скелету.

## Лицензия

Скелет получен из SMPL-модели, она распространяется по своей лицензии и требует
регистрации (`https://smpl.is.tue.mpg.de`). Здесь лежат **только имена, иерархия
и поза покоя** — ни весов, ни формы тела, ни меша SMPL в файле нет. Если дойдёт
до коммерческого релиза, лицензию SMPL надо перечитать отдельно; пометка стоит
здесь, чтобы вопрос не потерялся.

`LICENSE-godot-demo-projects.md` рядом относится к прежнему тестовому ригу
(`robot_rig.glb`, MIT) и оставлен как есть.

## Прежний риг `robot_rig.glb`

Робот из официального демо Godot (`3d/platformer`, MIT), 27 костей с IK-контролами
(`r-LEGCONTROL`, `l-ARMORIENT`) и нестандартными именами (`r-thigh`, `l-forearm`),
8 готовых клипов внутри (`idle`, `walk`, `run`, `jump`, `falling`, `shooting`,
`shooting_standing`, `RESET`).

Больше не эталонный: под мокап он не годится — ретаргет SMPL-22 на 27 костей
с контролами это отдельная работа, а собственные клипы робота к игре отношения
не имеют. Файл оставлен: он удобен как заведомо рабочий образец правильного
`.glb` со скелетом и анимациями, если надо с чем-то сверить импорт.
