---
id: task-0035
title: Одеяло: плоская часть без фактуры текстуры
assignee: houdini-assets
type: model
status: cancelled
revision: 1
priority: normal
created: 2026-09-02
started:
finished: 2026-09-02
---

## Отмена (заполняет Godot-сессия)

Ложная тревога, причина не в UV, а в интеграции: `.glb` из task-0029/task-0030
ссылались на текстуры внешним `uri` (`bedroom_albedo_1k.png` и т.д.), а сами
PNG не были скопированы в `game/assets/models/furniture/` — albedo был не
привязан вообще ни у одной поверхности ни одной из семи новых моделей
(проверено новым числовым инструментом `--texcheck=`: `albedo=false` везде).
Ровный цвет на плоской части одеяла — это просто нетекстурированная
геометрия, а не растянутые UV: там, где меша есть складки, форму давала
светотень от геометрии, без единого пятна текстуры.

После копирования PNG и переимпорта `--texcheck` показывает `albedo=true`,
1024×1024, разброс(σ)≈0.05 по всей UV-площади каждого меша, включая
`bed-single`/`bed-double` целиком (одеяло не выделено отдельным мешем —
меряется вместе с каркасом и матрасом в одной поверхности). Дефекта UV
не найдено, задание закрывается без правок ассета.

# task-0035 — Одеяло: плоская часть без фактуры текстуры

Не новый ассет — дефект на уже принятой сдаче task-0029 (`bedroom_set`),
файлы `bed_single.glb`/`bed_double.glb`, узел с одеялом (Vellum-сетка
18 × 21, 378 квадов).

## В чём дело

На кадре крупным планом (вид сверху, без разгона по перспективе) у изголовья,
где ткань собрана складками, фактура читается нормально — видна форма
и лёгкая светотень от самой геометрии. А на плоской части одеяла, где
ткань лежит ровно на матрасе (бо́льшая часть площади), — ровный кремовый
цвет без единого пятна, хотя в самой карте `bedroom_albedo_1k.png`
пятнистость/потёртости видны почти по всему атласу, включая области,
похожие на матрас и одеяло. Это не свет и не экспозиция — проверено кадром
без верхнего пересвета, разница осталась.

Подозреваю растянутые/уехавшие UV на плоской части сетки одеяла: у
Vellum-сетки после симуляции разворотка могла остаться той, что была на
плоском куске ткани ДО падения на кровать, и сильно растянутые треугольники
там, где ткань меньше всего деформировалась, размывают текстуру до
плоского цвета.

## Требования

- Проверить UV одеяла на `bed_single.glb`/`bed_double.glb`: нет ли
  сильно растянутых треугольников на ровной части (там, где ткань лежит
  плоско на матрасе).
- Если разворотка съехала при симуляции — перепаковать UV после Vellum,
  а не до.
- Каркас, матрас, тумбочку и комод не трогать — там всё в порядке.

## Формат сдачи

Как в `pipeline/ASSET_SPEC.md`. Обновлённые `bed_single.glb` и
`bed_double.glb`, тот же атлас `bedroom_*`.

## Приёмка (заполняет Godot-сессия после проверки)

### v1 — <дата>

- [ ] Пятнистость/фактура читается на всей поверхности одеяла, не только
      у складок
- [ ] Габарит и пивот не изменились
- [ ] Остальные три предмета (тумбочка, комод, вторая кровать) не задеты

Вердикт: принято / на переделку

## Правки (заполняет Godot-сессия при возврате на доработку)

Перечислять конкретно и по пунктам — исполнитель правит ровно это и не трогает остальное.

### → v2

1.
2.

## Вопросы (заполняет исполнитель, если задание уходит в blocked)
