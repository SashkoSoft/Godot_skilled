---
id: task-0034
title: Дверь room: полотно бликует металлом/стеклом
assignee: houdini-assets
type: model
status: done
revision: 1
priority: normal
created: 2026-09-01
started: 2026-09-06
finished: 2026-09-06
---

# task-0034 — Дверь room: полотно бликует металлом/стеклом

Не новый ассет — дефект на уже принятой сдаче task-0005 (`door_blocks`), файл
`door_room.glb`. Полотно (узел `leaf`), поставленное распахнутым, при боковом
свете читается как мутное синеватое стекло/зеркало по всей площади — не
дерево. Дверь простая filenka (без стеклянной вставки по заданию), стекла в
ней быть не должно.

## Замер, чтобы не гадать вслепую

По `door_blocks_orm_1k.png` металлу и блеску отдано **0.08 %** атласа (881 из
1 048 576 пикселей с B>100, ещё столько же с G<150) — это ровно бюджет
петель/ручки/глазка, как и написано в RESULT.md сдачи. То есть **в текстуре
дефекта нет** — блик на всём полотне в движке шире, чем блеск в самих данных.

Подозреваю одно из двух:
1. **UV полотна `leaf`** — если развёртка съезжает на маленький кусок атласа
   с ручкой/петлёй, вся площадь потянет их блеск.
2. **Конвенция нормали.** В RESULT.md task-0005 сказано прямо: «конвенция
   нормали импортом в движок не подтверждена, проверено косвенно». Если ось Y
   перевёрнута, блик может лечь широкой полосой даже без реального металла —
   это чаще всего выглядит именно как «мутное стекло» под острым углом света.

Проверить снаружи не могу: текстуры зашиты в бинарник `.glb`, а не лежат
рядом файлами — подменить normal-карту руками, чтобы проверить гипотезу (2),
нечем.

## Кадр дефекта

Полотно двери из прихожей в жилую, повёрнуто на ~30°, боковой свет от окна.
Видно на прикреплённом кадре (пришлю в чат/приложу к сдаче) — вся видимая
грань полотна серо-синяя и глянцевая, рама и остальная геометрия — нет.

## Требования

- Проверить UV полотна `leaf` в `door_room.glb` (и заодно `door_flat.glb`,
  `door_broken.glb` — тот же атлас, тот же риск).
- Проверить ось Y нормали на реальном экспорте в Godot, а не только
  косвенно — конвенцию из RESULT.md пора подтвердить, раз она уже второй раз
  под подозрением.
- Если находится баг геометрии/UV — поправить и пересдать эти три `.glb`.
  Текстуры (`door_blocks_*`) трогать не нужно, если дело не в них.

## Приёмка (заполняет Godot-сессия после проверки)

### v1 — 2026-09-06

Не проверка ассета, а закрытие расследования — семь версий, все сняты
числами (полный список в RESULT.md сдачи). Своей стороной добавил две:
сам отрендерил два разных экземпляра `door_room` прямо в собранной
сцене (нашёл координаты через `--list-doors`) — оба матовые; и
рассмотрел, не спутан ли блик со стеной прихожей рядом (там тёмная,
чуть глянцевая полоса краски) — её ORM тоже без аномалий (шероховатость
0.51–0.70 после множителя, металл всюду 0).

Решающий кадр — houdini-assets прогнала сам `door_room.glb` через
скользящий свет 55°→5°: пересвет не растёт к острому углу (0.25%→0.37%,
а не кратно, как дал бы зеркальный отклик), B−R везде отрицательный
(полотно теплее нейтрали, а жалоба была на синеву). На кадрах при этом
есть тёмная полоса и мягкое тёмное пятно, издали похожие на «мутное
стекло» — но это контактная тень от четверти дверной коробки, она сама
меняет форму между 14° и 55° (сравнил `sheen_14deg.png`/`sheen_55deg.png`
глазами — сдвигается), к материалу отношения не имеет. Вероятно, это
и приняли за блик в исходном (утерянном) кадре.

- [x] Полотно матовое при скользящем свете от 55° до 5° — подтверждено
      числами, не только визуально
- [x] Металл/блеск только на фурнитуре — 0.07% площади под UV `leaf`
- [x] Конвенция нормали подтверждена напрямую (не косвенно, как раньше):
      длина векторов 1.000, z>0 везде

Файлы ассета не менял — нечего было менять.

Вердикт: **закрыто как невоспроизводимое** (не «исправлено» — баг не
найден ни в файлах, ни в живой сцене). Вернуть одной строкой, если
появится новый кадр: `scripts/check_door_sheen.py --angles`.

## Правки (заполняет Godot-сессия при возврате на доработку)

Перечислять конкретно и по пунктам — исполнитель правит ровно это и не трогает остальное.

### → v2

1.
2.

## Вопросы (заполняет исполнитель, если задание уходит в blocked)
