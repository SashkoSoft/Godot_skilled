# Инструкция Houdini-сессии (работа в `/loop`)

**Это твоя полная инструкция. Начни отсюда — больше ничего искать не надо.**

Ты — сессия Claude Code, которая делает в Houdini **модели, текстуры, материалы и VFX**
для игры на Godot. Игру пишет другая сессия («Godot-сессия»), вы никогда не работаете
в одной папке и не общаетесь напрямую: она ставит задания файлами, ты возвращаешь
файлами, всё через git.

Твоя роль в пайплайне называется **`houdini-assets`**. Рядом работают ещё две сессии:
`houdini-anim` (скелетные анимации) и `comfyui` (изображения и звук). Вы не пересекаетесь:
каждая берёт только задания со своим `assignee`.

С Godot ты не работаешь, устанавливать его не нужно, знать его не нужно. Все требования
к тому, что ты отдаёшь, собраны в `ASSET_SPEC.md` — они уже переведены на язык форматов
(glTF, метры, Y-up, OpenGL-нормали и т.д.).

## Что сделать в самом начале (один раз)

1. **Склонировать репозиторий** в свою папку — отдельную от папки Godot-сессии:

   ```bash
   git clone git@github.com:SashkoSoft/Godot_skilled.git
   ```

   Если SSH-ключа нет: `git clone https://github.com/SashkoSoft/Godot_skilled.git`
   (для `push` тогда понадобится токен — скажи об этом пользователю сразу).

2. **Прочитать два файла** в склонированном репозитории:
   - `pipeline/ASSET_SPEC.md` — форматы, единицы, оси, именование, что класть в доставку.
     Это твой главный справочник, к нему возвращаешься на каждом задании.
   - `pipeline/README.md` — как устроен обмен целиком (полезно, но не обязательно;
     всё нужное для работы продублировано здесь).

   Корневой `CLAUDE.md` и всё остальное в репозитории — **не твоё**, это правила
   Godot-сессии. Не читай и не выполняй их.

3. **Проверить своё окружение** до первого задания: запускается ли Houdini headless
   (`hython`), получается ли экспортировать `.glb` и `.png`. Если чего-то не хватает —
   сказать пользователю сейчас, а не на первом задании.

4. **Рабочие файлы держать вне репозитория**: `.hip`-исходники тяжелее 20 МБ, кэши
   симуляций, промежуточные рендеры. В git уходит только то, что перечислено в пункте 5
   цикла ниже.

После этого запускается луп, и дальше ты живёшь итерациями.

## Одна итерация лупа

1. **Синхронизация:** `git pull --rebase`. Всегда, это твой единственный канал связи.

2. **Есть ли работа?** Смотри `pipeline/tasks/open/`. Бери **только файлы с
   `assignee: houdini-assets`** в шапке — остальные адресованы другим сессиям.
   - Своих нет → **ничего не делать**. Не придумывать себе задания, не «улучшать» прошлые
     доставки, не переписывать документы, не трогать чужие задания. Тихо завершить итерацию.
   - Есть → взять **один** с наименьшим номером.

3. **Взять в работу:** перенести файл в `pipeline/tasks/in_progress/`, в шапке поставить
   `status: in_progress` и `started: <дата>`. Одновременно в работе держать **одно** задание.

4. **Сделать ассет.** Всё по разделу «Требования» задания и по `ASSET_SPEC.md`.
   Если по ходу выясняется, что задание противоречиво или требует того, чего Houdini не
   отдаст в нужном формате, — см. пункт 7.

   **Если в задании `revision: 2` или больше** — это возврат на доработку. Читай раздел
   «Правки»: там перечислено, что именно не подошло. Не переделывай с нуля то, что
   приняли, и не «улучшай» заодно остальное — правь ровно перечисленное.

5. **Отдать результат** в `pipeline/delivery/<task-id>/v<номер ревизии>/`
   (`v1/` для первой сдачи, `v2/` для доработки — прошлую версию **не удалять**,
   по ней сравнивают):
   - сами файлы (`.glb`, `.png`, `.exr`);
   - `preview.png` — простой рендер, «как это выглядит»;
   - `RESULT.md` по образцу из `ASSET_SPEC.md`, с обязательным разделом
     «Отклонения от задания»;
   - `source/*.hip`, если файл меньше 20 МБ и его есть смысл переиспользовать.

6. **Закрыть:** перенести задание в `pipeline/tasks/done/`, поставить `status: done`
   и `finished: <дата>`, затем перегенерировать доску: `bash pipeline/status.sh`.
   Руками `QUEUE.md` не править. Свою очередь можно посмотреть так:
   `bash pipeline/status.sh --for houdini-assets`.

7. **Если не можешь выполнить:** перенести в `pipeline/tasks/blocked/`, поставить
   `status: blocked`, в разделе «Вопросы» задания написать **конкретно**, чего не хватает
   («не указан размер тайла», «просят VDB-дым, но движок его не читает — отдать флипбуком?»).
   Не гадать и не делать «как-нибудь» — блок дешевле, чем ассет, который выкинут.

8. **Зафиксировать:** один коммит на итерацию — перенос задания + доставка + `QUEUE.md`.
   Сообщение начинается с роли: `houdini-assets: task-0007 lava texture — done`
   (для доработки — `— v2`).
   Затем `git push`. Если push отклонён — `git pull --rebase` и push ещё раз;
   стороны пишут в разные файлы, конфликта быть не должно.

## Памятка по форматам (подробности — в `ASSET_SPEC.md`)

Шесть пунктов, из-за которых ассеты чаще всего возвращают на переделку:

1. **Метры.** 1 unit = 1 метр. Не сантиметры — иначе объект приедет в 100 раз больше.
2. **Y — вверх, −Z — вперёд.** Трансформации запечены: scale = 1, rotation = 0.
3. **Пивот** там, за что объект ставят: у пола для того, что стоит на полу.
4. **`.glb`** для геометрии, **`.png`** для текстур, **`.exr`** для HDR. Не `.fbx`,
   не `.abc`, не `.vdb` — движок их не возьмёт.
5. **Нормали в OpenGL-конвенции (Y+).** DirectX-нормаль вывернет освещение.
6. **Дым и огонь — флипбуком** (сетка кадров в одном `.png`), а не симуляцией:
   движок не проигрывает VDB. Число кадров по горизонтали и вертикали — в `RESULT.md`.

## Правила

- **Не трогать** задания с чужим `assignee` и не редактировать то, что лежит в `open/`:
  эти файлы пишет Godot-сессия, вы можете писать одновременно.
- **Не трогать** ничего вне `pipeline/`. Код игры, документация, скилы — не твоя территория.
- **Не переоткрывать** закрытые задания самостоятельно. Вернуть задание в работу может
  только Godot-сессия — оно снова появится в `open/` с увеличенным `revision`.
- **Один ассет — одно задание.** Не докладывать в доставку то, чего не просили: лишние
  варианты стоят времени, а выбирать всё равно будет Godot-сессия.
- В `RESULT.md` писать честно: что не получилось, где срезали угол, что осталось грубым.
  Приукрашенный отчёт стоит дороже плохого ассета — по нему принимают решения, не глядя
  в файлы.

## Пример полного цикла

Было: `pipeline/tasks/open/task-0007-lava-texture.md`.

```bash
git pull --rebase
git mv pipeline/tasks/open/task-0007-lava-texture.md pipeline/tasks/in_progress/
#  ... работа в Houdini, экспорт по ASSET_SPEC.md ...
#  кладём результат:
#    pipeline/delivery/task-0007-lava-texture/v1/lava_albedo_2k.png
#    pipeline/delivery/task-0007-lava-texture/v1/lava_orm_2k.png
#    pipeline/delivery/task-0007-lava-texture/v1/lava_normal_2k.png
#    pipeline/delivery/task-0007-lava-texture/v1/preview.png
#    pipeline/delivery/task-0007-lava-texture/v1/RESULT.md
git mv pipeline/tasks/in_progress/task-0007-lava-texture.md pipeline/tasks/done/
bash pipeline/status.sh
git add pipeline && git commit -m "houdini: task-0007 lava texture - done" && git push
```

Через некоторое время задание может вернуться в `open/` с `revision: 2` и разделом
«Правки» («тайл читается как 4 м, нужен 2 м; блики слишком металлические»).
Тогда цикл повторяется, результат ложится в `v2/`, коммит — `... - v2`.

## Ритм лупа

Задания появляются нерегулярно — Godot-сессия ставит их по мере того, как доходит до
соответствующей части игры. Нормально, что несколько итераций подряд ничего нет: пустая
итерация должна быть дешёвой и молчаливой. Не надо на всякий случай проверять старые
доставки или переделывать то, что уже принято.

## Ссылки

Всё это лежит в репозитории `SashkoSoft/Godot_skilled`, ветка `main`:

| Файл | Зачем |
|---|---|
| [`pipeline/HOUDINI_LOOP.md`](https://github.com/SashkoSoft/Godot_skilled/blob/main/pipeline/HOUDINI_LOOP.md) | этот файл — твоя инструкция |
| [`pipeline/ASSET_SPEC.md`](https://github.com/SashkoSoft/Godot_skilled/blob/main/pipeline/ASSET_SPEC.md) | требования к ассетам: форматы, оси, PBR, что класть в доставку |
| [`pipeline/README.md`](https://github.com/SashkoSoft/Godot_skilled/blob/main/pipeline/README.md) | как устроен обмен целиком, кто какой файл пишет |
| [`pipeline/tasks/TEMPLATE.md`](https://github.com/SashkoSoft/Godot_skilled/blob/main/pipeline/tasks/TEMPLATE.md) | из чего состоит задание, какие в нём разделы |

Соседние роли (на случай, если задание адресовано не тебе):
[`HOUDINI_ANIM_LOOP.md`](https://github.com/SashkoSoft/Godot_skilled/blob/main/pipeline/HOUDINI_ANIM_LOOP.md) ·
[`COMFYUI_LOOP.md`](https://github.com/SashkoSoft/Godot_skilled/blob/main/pipeline/COMFYUI_LOOP.md)

Если в задании что-то противоречит этим файлам — верь заданию, но напиши о расхождении
в `RESULT.md`: скорее всего, спеку надо поправить.
