# Разнообразие из немногих ассетов: что решает сборка, а что заказ

Сцена, собранная из кода, повторяется сильнее ручной: один и тот же материал
ложится на все стены, одна модель встаёт во все проёмы. Ниже — что с этим
делать, в порядке от дешёвого к дорогому.

## Три источника непохожести, и только один дорогой

| источник | цена | что даёт |
|---|---|---|
| поворот и отражение тайла | ноль | ×4 на каждом наборе без вертикали |
| слои: чистая база + декали | почти ноль | локальные повреждения там, где нужно |
| набор вариантов | сдача за каждый | разные рисунки, разные состояния |

Порядок работы обратный интуиции: **сначала выжать поворот и слои, и только
потом заказывать новые наборы**. Иначе очередь исполнителей забита тем, что
можно было получить строчкой кода.

## Что можно крутить, а что нельзя

Тайл поворачивается на 90/180/270 и зеркалится, если у него нет **вшитой
вертикали**:

- крутится: паркет, плитка, линолеум, бетон, потолок, штукатурка;
- не крутится: обои (полотнище вертикально), крашеная стена с панелью до пояса,
  всё с бордюром или направленным потёком.

Это должно быть сказано в задании явно — «рисунок без вертикали, обязан
тайлиться после поворота на 90°». Вшитая вертикаль там, где её не просили,
отнимает три четверти пользы от набора, и понимаешь это только на сборке.

## База чистая, износ отдельно

Локальное повреждение, вшитое в тайл, повторяется через каждый метр и читается
как узор, а не как повреждение. Поэтому:

- в базе — только **равномерное**: выцветание, затёртость, разнотон;
- пятно, скол, потёк, плесень — **декаль**, ставится в конкретное место.

Проверка одна: на укладке 4 × 4 глаз не должен находить повторяющуюся примету.

Отсюда же **два состояния** вместо пяти: `целое` и `битое`. Промежуток
набирается декалями, и это дешевле, чем три лишние сдачи.

## Вещь говорит, где она стоит

Ассет несёт смысл о своём месте, и поставить его не туда — грубее, чем
поставить некрасивый. Дверь, обитая дерматином, — входная; филёнчатая —
межкомнатная; с остеклением вверху — балконная.

**Выбирать модель по размеру — ошибка, которая обязательно выстрелит.** У нас
проём между комнатами 0.94 оказался шире входного 0.88, и правило
`if width >= 0.86: входная` поставило дерматиновую дверь внутрь квартиры.

Правильно — по соседям проёма: посмотреть, что за помещения лежат по обе
стороны, и решить по ним.

```gdscript
func _door_role(cx: float, cz: float, along_z: bool) -> DoorRole:
	var p := 0.40
	var a := _kind_at(cx - p, cz) if along_z else _kind_at(cx, cz - p)
	var b := _kind_at(cx + p, cz) if along_z else _kind_at(cx, cz + p)
	if a == "лоджия" or b == "лоджия":
		return DoorRole.BALCONY
	if a == "" or b == "":            # снаружи помещения нет — выход из квартиры
		return DoorRole.ENTRANCE
	return DoorRole.ROOM
```

Из этого же следует требование к исполнителю: **называть модели по назначению**
(`door_flat`, `door_room`), а не по размеру (`door_090`). Имя по размеру
провоцирует выбор по размеру.

## Стохастический тайл: рабочий шейдер

Три приёма в одном материале, каждый бьёт свой масштаб повторения.

**1. Стохастическая выборка (Heitz & Neyret).** Плоскость режется треугольной
решёткой; в каждой точке берутся три копии текстуры со случайными сдвигами и
смешиваются по барицентрическим весам. Сетка тайла исчезает совсем.

```glsl
void triangle_grid(vec2 uv, out vec3 w, out vec2 v1, out vec2 v2, out vec2 v3) {
	uv *= 3.4641016;                       // 2 * sqrt(3)
	mat2 to_skew = mat2(vec2(1.0, -0.57735027), vec2(0.0, 1.15470054));
	vec2 skewed = to_skew * uv;
	vec2 base = floor(skewed);
	vec3 t = vec3(fract(skewed), 0.0);
	t.z = 1.0 - t.x - t.y;
	if (t.z > 0.0) { w = vec3(t.z, t.y, t.x);
		v1 = base; v2 = base + vec2(0,1); v3 = base + vec2(1,0);
	} else { w = vec3(-t.z, 1.0 - t.y, 1.0 - t.x);
		v1 = base + vec2(1,1); v2 = base + vec2(1,0); v3 = base + vec2(0,1); }
}
```

Два подводных камня:

- **Линейное смешивание съедает контраст.** Лечится возведением весов в
  степень (`pow(w, vec3(7.0))` с нормировкой): переход становится почти
  выбором, контраст на месте, шва не видно. Полное гистограмм-сохраняющее
  смешивание из статьи для игрового интерьера избыточно.
- **Мипы.** Обычный `texture()` посчитает производные по сдвинутым координатам
  и даст швы на дальних планах. Нужен `textureGrad` с производными исходного
  UV.

**2. Привязка сдвига к внутренней сетке.** Если в наборе плитка или щитовой
паркет, случайный сдвиг разрежет шов. Сдвиг округляется до шага сетки:

```glsl
if (snap_cells > 0) {
	float k = float(snap_cells);
	o1 = floor(o1 * k) / k;   // и так же o2, o3
}
```

**3. Макро-вариация и карта id.** Крупный шум с шагом в метрах, **не кратным
тайлу**, гуляет светлотой на ±9 % — это ломает ощущение одинакового поля. Карта
id (каждый элемент залит своим значением) даёт каждой планке свой оттенок; без
неё все паркетины в тайле одного цвета, и это видно на большой площади.

**Чему стохастика противопоказана:** всё с вшитой вертикалью — обои, краска
с панелью по высоте. Сдвиг увёл бы панель с её отметки.

## Суперсэмплинг для превью

MSAA сглаживает только кромки геометрии. Рябь текстуры под скользящим углом и
мерцание тонких переплётов убирает только выборка нескольких пикселей на один:
рисовать SubViewport вдвое крупнее и уменьшать на сохранении
(`img.resize(w, h, Image.INTERPOLATE_LANCZOS)`). Для неподвижного кадра это
дешевле и честнее, чем TAA, который на статике всё равно ничего не накапливает.

## Расстановка предметов от плана, а не координатами

Если предметы расставлены списком координат, зеркальная половина дома и
следующий этаж придётся расставлять заново. Считать надо от разбора:

**Спиной к стене.** Пробуем четыре направления и берём то, где помещение
кончается:

```gdscript
func _wall_dir(x: float, z: float, reach: float) -> Vector3:
	for d in [Vector3(0,0,1), Vector3(0,0,-1), Vector3(1,0,0), Vector3(-1,0,0)]:
		if _kind_at(x + d.x * reach, z + d.z * reach) == "":
			return d
	return Vector3(0, 0, 1)
```

Дальше `yaw = atan2(wd.x, wd.z)`, если у моделей лицо смотрит в −Z (обычная
договорённость в спеке).

**Дверь маленького помещения открывается наружу.** В ванной 1.34 × 1.63 и
уборной 0.71 × 1.63 полотну просто некуда распахнуться внутрь. Сторону
определяем по соседям проёма, а направление проверяем по факту: куда уехал
кончик полотна после поворота — туда и открылась дверь.

```gdscript
var tip := leaf.global_transform * Vector3(0.7, 1.0, 0.0)
if (tip - door_center).dot(out_dir) < 0.0:
	leaf.rotation.y -= angle * 2.0        # развернуть в другую сторону
```

Так не нужно угадывать конвенцию модели: проверяем результат, а не соглашение.

**Выступающие части мешают.** Штора висела в плоскости стены и проходила
сквозь подоконник: у оконного блока габарит по толщине 0.48 при стене 0.34,
доска выступает в комнату на 0.07. Правило: вешая что-то у проёма, брать
глубину не стены, а **блока**, который в этот проём вставлен.

**Ниша не может быть частью помещения.** Кладовка «6а» перекрывалась с
прямоугольником прихожей на 0.30 × 0.67 м, и на этом куске в коридоре стояла
полноростовая коробка. Прямоугольники комнат надо отсекать по нишам — сдвигая
границу по той оси, где перекрытие меньше. Проверка: пересечений помещений с
нишами ноль.

## Декаль не должна читаться штампом

Локальное пятно выдаёт себя тремя вещами: резкой кромкой маски, слишком сильным
смешиванием и одинаковой ориентацией. Против всех трёх:

```gdscript
d.albedo_mix = 0.55        # затёртость — грязь в порах, а не наклейка
d.upper_fade = 2.0         # гасить к краю проекции
d.lower_fade = 2.0
d.modulate = Color(0.86, 0.84, 0.81)   # разница с полом заметна, но не спорит
```

И ориентация: пятну на полу можно дать любой угол, потёку и плесени — нельзя,
у них есть верх. Вытертой тропе нужен **конкретный** угол вдоль прохода, а не
случайный: поперёк она читается кляксой. Поэтому в API лучше передавать угол
числом, а «крутить как угодно» кодировать отдельным значением, чем ставить
булев флаг «крутить или нет» — с флагом у тропы остаётся выбор из двух
положений вместо одного нужного.

**Затёртое место темнее пола, а не светлее.** Светлая декаль вытертости на
буром паркете просто не видна. В жизни лак сходит, в поры набивается грязь.

## Выбор варианта должен быть устойчивым

Вариант выбирается детерминированно — от координаты или от индекса помещения,
никогда от `randi()`. Иначе дом меняется между запусками, и нельзя ни сравнить
два кадра, ни воспроизвести жалобу.

```gdscript
var seed_v := int(absf(pos.x) * 71.0 + absf(pos.z) * 131.0) % 100
```

Множители — простые числа, чтобы соседние проёмы не попадали в один класс.

## Что варьировать, а что нет

Полезно решить это один раз и записать: в панельном доме **оболочка типовая,
начинка разная**. Стены, окна, двери подъезда — по одному экземпляру достаточно.
Всё, до чего жильцы дотягивались руками, — разное: обои, пол, плитка, дверное
полотно, светильник.

Практическое следствие: наборов обоев нужно 4 на 8 квартир этажа, а плитки
санузла хватает одной — её меняли реже всего.

## Масштаб — в метрах, и это часть сдачи

Тайлящийся набор без размера тайла в метрах не встаёт вообще: материал кладётся
трипланарно, `uv1_scale = 1 / размер`. Требовать `tiles.txt` (`имя размер
описание`) и `sizes.txt` для декалей — машиночитаемо, чтобы код читал файл, а не
цифру из переписки.

Заодно это ловит ошибки плотности: паркетная планка 5 × 20 см, плитка 15 см,
полотнище обоев 53 см. Если тайл обоев 1.06 — это ровно два полотнища.
